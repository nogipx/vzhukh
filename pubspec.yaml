name: vzhukh
description: "SSH tunnel VPN for Android — your server, your traffic."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.9
  dartssh2: ^2.16.0
  ffi: ^2.2.0
  flutter_secure_storage: ^10.0.0
  pinenacl: ^0.6.0
  pointycastle: ^4.0.0
  qr_flutter: ^4.1.0
  mobile_scanner: ^5.0.0
  connectivity_plus: ^7.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
